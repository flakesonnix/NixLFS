# garnix.yaml configuration for NixLFS
# https://garnix.io

systems:
  - x86_64-linux

inputs:
  nixpkgs:
    url: github:NixOS/nixpkgs/nixos-24.05

outputs:
  - path: packages.x86_64-linux.lfsSystem
