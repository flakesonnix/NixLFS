# garnix.yaml configuration for NixLFS
# https://garnix.io

systems:
  - x86_64-linux

inputs:
  nixpkgs:
    url: github:NixOS/nixpkgs/nixos-24.05

# Only build the final system (kernel)
# This transitively builds everything else
outputs:
  - path: packages.x86_64-linux.sysconf.kernel
